'use client';

import React from 'react';
import SettingsLayout from '@/layouts/settings/layout';

export default function AppSettingsLayout({ children }) {
    return <SettingsLayout>{children}</SettingsLayout>;
}
